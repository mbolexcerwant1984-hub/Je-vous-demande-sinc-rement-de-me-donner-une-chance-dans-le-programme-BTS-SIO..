<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Page de Contact - Daniil</title>
    <style>
        /* Styles principaux */
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f0f4f8;
            color: #333;
            transition: background-color 0.5s ease, color 0.5s ease;
        }

        h1 {
            text-align: center;
            font-size: 48px;
            color: #005f73;
            margin-top: 50px;
            transition: color 0.5s ease;
        }

        button {
            padding: 12px 28px;
            background-color: #006d77;
            color: #fff;
            border: none;
            border-radius: 8px;
            font-size: 18px;
            cursor: pointer;
            transition: all 0.3s ease;
            margin-top: 20px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }

        button:hover {
            background-color: #008f94;
            transform: scale(1.1);
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.3);
        }

        /* Styles de formulaire */
        form {
            background-color: #fff;
            padding: 30px;
            border-radius: 8px;
            width: 400px;
            margin: 40px auto;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        input, textarea {
            width: 100%;
            padding: 12px;
            margin: 8px 0;
            border-radius: 6px;
            border: 1px solid #ccc;
            font-size: 16px;
            transition: border 0.3s ease;
        }

        input:focus, textarea:focus {
            border: 1px solid #006d77;
            outline: none;
        }

        textarea {
            height: 150px;
        }

        #charCount {
            font-size: 14px;
            color: #555;
        }

        /* Mode sombre */
        body.dark {
            background-color: #121212;
            color: #eaeaea;
        }

        body.dark h1 {
            color: #fff;
        }

        body.dark button {
            background-color: #33b5b3;
        }

        body.dark button:hover {
            background-color: #00b3a6;
        }

        body.dark form {
            background-color: #1d1d1d;
            color: #eaeaea;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
        }

        body.dark input, body.dark textarea {
            background-color: #333;
            color: #fff;
            border: 1px solid #555;
        }
    </style>
</head>
<body>
    <h1>Bienvenue sur ma page !</h1>

    <!-- Bouton de mode sombre/clair -->
    <button id="themeToggleBtn" onclick="toggleTheme()">Passer en mode sombre/clair</button>

    <!-- Bouton interactif -->
    <button id="magicBtn" onclick="changeColor()">Cliquez-moi !</button>

    <!-- Formulaire -->
    <form id="formulaireDaniil" onsubmit="return verifForm()">
        <h2>Contactez-moi</h2>
        <input type="text" id="monNom" placeholder="Votre nom" required>
        <input type="email" id="monEmail" placeholder="Votre email" required>
        <textarea id="monMessage" placeholder="Votre message" oninput="updateCharCount()" required></textarea>
        <div id="charCount">0 caractères</div>
        <button type="submit">Envoyer</button>
    </form>

    <div id="smiley" style="display:none;">😊</div>

    <script>
        // Fonction de bascule entre le mode sombre et clair
        function toggleTheme() {
            const body = document.body;
            body.classList.toggle("dark");

            const button = document.getElementById("themeToggleBtn");
            button.textContent = body.classList.contains("dark") ? "Passer en mode clair" : "Passer en mode sombre";
        }

        // Fonction pour changer la couleur du bouton
        function changeColor() {
            const btn = document.getElementById("magicBtn");
            const colors = ["#006d77", "#33b5b3", "#ff9f1c", "#ff5c8d", "#8ac926"];
            const randomColor = colors[Math.floor(Math.random() * colors.length)];
            btn.style.backgroundColor = randomColor;
        }

        // Fonction de validation du formulaire
        function verifForm() {
            let nom = document.getElementById("monNom").value;
            let email = document.getElementById("monEmail").value;
            let message = document.getElementById("monMessage").value;

            if (!nom || !email || !message) {
                alert("Tous les champs doivent être remplis !");
                return false;
            }

            alert("Merci " + nom + " ! Votre message a été envoyé.");
            document.getElementById("smiley").style.display = "inline";
            return false;
        }

        // Fonction pour mettre à jour le compteur de caractères
        function updateCharCount() {
            const message = document.getElementById("monMessage").value;
            document.getElementById("charCount").textContent = message.length + " caractères";
        }
    </script>
</body>
</html>
